var React = require('react');

var Home = React.createClass({
    render:function(){
        return (
            <h2 className="text-center">
                通过github的用户名搜索
            </h2>
        )
    }
})

module.exports = Home;